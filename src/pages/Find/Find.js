// @app
import React, {
  useEffect,
  useState
} from 'react'; 
import Header from '../../components/Header';

const Find = ({ navigation }) => { 
  return (
    <Header title={`Find`} />

  );
};
export default Find;
